import { registerRootComponent } from 'expo';
import App from './App';

/* registerRootComponent ensures the environment is set up appropriately
 * whether you load the app via Expo Go or a standalone build. */
registerRootComponent(App);
